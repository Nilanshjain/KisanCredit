# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
httpx==0.24.1

# Code Quality
black==23.11.0
ruff==0.1.6

# Load Testing
locust==2.17.0

# Notebooks
jupyter==1.0.0
matplotlib==3.8.2
seaborn==0.13.0

# Phase 5 — Home Credit retrain (offline / notebook-only; not shipped to Render)
kaggle==1.6.17                          # dataset download
optuna==3.5.0                           # LightGBM hyperparameter tuning
pytorch-tabnet==4.1.0                   # DL comparison baseline (uses your existing torch)
